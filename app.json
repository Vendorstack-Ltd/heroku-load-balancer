{
  "name": "heroku-load-balancer",
  "description": "Cheaper than built-in, the load balancer for your Heroku pipeline applications. The way to host CPU-bound",
  "repository": "https://github.com/dmytrostriletskyi/heroku-load-balancer",
  "env": {
    "HEROKU_API_KEY": {
      "description": "Deprecated / unused. The backend is now a fixed upstream in the Nginx config; kept only for backwards compatibility.",
      "required": false
    },
    "PIPELINE_IDENTIFIER": {
      "description": "Deprecated / unused. The backend is now a fixed upstream in the Nginx config; kept only for backwards compatibility.",
      "required": false
    }
  },
  "logo": "https://habrastorage.org/webt/w3/fp/ep/w3fpepqfjjtumhnyul4ymts-qm8.png",
  "keywords": ["nginx", "docker", "proxy", "balancing", "balancer"],
  "stack": "container"
}
